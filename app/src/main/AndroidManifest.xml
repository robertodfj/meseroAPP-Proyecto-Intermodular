<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/m3_split_button_chevron_avd"
        android:label="@string/app_name"
        android:roundIcon="@drawable/m3_split_button_chevron_avd"
        android:supportsRtl="true"
        android:theme="@style/Theme.MESEROAPP">

        <!-- Esta será la primera que se ejecute -->
        <activity
            android:name=".Login.LoginRegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Main.MainActivity"
            android:exported="false" />

    </application>

</manifest>